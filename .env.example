# =============================================================================
# GovAI Environment Configuration
# =============================================================================
#
# Quick Start (for judges/evaluators):
# 1. Copy this file to .env: cp .env.example .env
# 2. Run: docker-compose up --build
# 3. Open http://localhost:3000 and go to Governance page
# 4. Enter your Gemini API key in the UI
#
# NEVER commit .env or .env.local to version control
# =============================================================================

# =============================================================================
# GEMINI API KEY - Configure via Governance Dashboard
# =============================================================================
# API key is NO LONGER read from this file for security.
# Enter your key at: http://localhost:3000 → Governance → API Configuration
# Get a free API key from: https://aistudio.google.com/app/apikey
#
# GEMINI_API_KEY is intentionally not loaded from environment

# =============================================================================
# OPTIONAL: Authentication Settings
# =============================================================================
# GovAI API Key - for protecting backend endpoints
# Generate a secure random key: openssl rand -base64 32
# Leave empty or set DISABLE_AUTH=true for demo mode
GOVAI_API_KEY=

# Disable authentication for demo/evaluation (default: true for docker-compose)
# Set to "false" to require GOVAI_API_KEY for all requests
DISABLE_AUTH=true

# =============================================================================
# OPTIONAL: Model Configuration
# =============================================================================
# Override default LLM models (uses Gemini by default)
# LLM_FAST_MODEL=gemini-2.5-flash-lite
# LLM_REASONING_MODEL=gemini-2.5-pro
# LLM_VISION_MODEL=gemini-2.5-flash
# LLM_AUDIO_MODEL=gemini-2.5-flash

# =============================================================================
# OPTIONAL: Backend Settings
# =============================================================================
# CORS origins (comma-separated list of allowed origins)
# CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# Maximum file upload size in MB (default: 10)
# MAX_UPLOAD_SIZE_MB=10

# =============================================================================
# OPTIONAL: Frontend Settings (Vite)
# =============================================================================
# Backend API URL (used by frontend to connect to backend)
# For Docker: VITE_RAG_API_URL=http://backend:8000
# For local dev: VITE_RAG_API_URL=http://localhost:8000
# VITE_RAG_API_URL=http://localhost:8000

# Frontend API key (should match GOVAI_API_KEY if auth is enabled)
# VITE_GOVAI_API_KEY=
